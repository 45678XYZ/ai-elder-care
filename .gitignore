# --- Flutter / Dart ---
# 全字庫原始字型（共約 120 MB）只是拿來裁子集的來源，不進版控。
# 實際打包的是 assets/fonts/TWSungHakka.ttf（2.4 KB），那個要進。
app/assets/fonts/TW-Sung-*.ttf
app/.dart_tool/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/local.properties
app/ios/Pods/
app/*.iml

# --- Python ---
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.hypothesis/
*.egg-info/
backend/chroma_db/

# --- Terraform ---
terraform/.terraform/
terraform/build/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
# plan 產物會夾帶變數值與資源屬性，且每次 plan 都不同，不進版控
tfplan
*.tfplan

# --- AI tool configs (local, per-developer) ---
# 給自己看的交接筆記，不進版控。
#
# app/CLAUDE.md **不在這裡**：它雖然是 AI 助手的約束檔，實際上是全隊在維護的
# 技術約束文件（登入用哪個套件、UI 硬性規格等），別人也會改。排除它會讓隊友
# 的修改從 repo 消失。
HANDOFF.md
.claude/
.codex/
.cursor/
.windsurf/
.continue/
.aider*
.gemini/
.qwen/
.roo/
.agents/*
!.agents/skills/
graphify-out/
CLAUDE.md

# --- 離線訓練工作流產出（語料、embedding 快取、標註檔、評測結果） ---
# 內含第三方語料的衍生資料（DialSeg711／TIAGE／SeniorTalk），授權不允許再散布，一律不入庫
data/segmenter_v2/
backend/local_runner/

# --- 建置產出 ---
# scripts/build_kb_upload.py 產生的 KB 上傳版本（正文 + metadata sidecar）；
# 由 data/knowledge/ 每次重新產生，不是原始資料
build/

# --- 本地交付文件（不進版控） ---
docs/deliverables/

# --- misc ---
.DS_Store
.env

# --- 本地端測試與 Runner 產出 (Local Runner & Offline Pipeline) ---
backend/local_runner/data/
backend/local_runner/outputs/
backend/local_runner/storage/
.env.local
