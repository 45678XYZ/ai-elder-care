# 長者模式 逐頁規則

共同硬約束見 [../../CLAUDE.md](../../CLAUDE.md)「長者模式」段（內文 ≥24sp、touch ≥60dp、單頁 ≤3 互動、語音替代、無隱藏手勢）。

## 今日（today_screen）
- 頂部農民曆牌面：干支年與生肖、大朱紅日期（46/66 級）、朱紅分隔線、農曆月日；當天是節氣或農曆節日時另出朱紅膠囊（節日優先於節氣）
- **不做「宜／忌」**：那是健康建議，不該由 App 端用規則推導；要做也應由後端依對話與事件產生
- 農曆換算走 `lunar` 套件，但它輸出簡體，一律經 `shared/services/lunar_date.dart` 轉繁再顯示
- 下方當日行程（GET `/routines?elder_id=&date=`），可手動確認完成（POST `/routines/{id}/complete`）
- 為守住「單頁可互動元素 ≤3」，只有「接下來那一件」給確認按鈕，其餘行程純顯示不可點
- 不放語言切換：介面一律華語，語音語言由照護者在初次設定或「管理」頁決定，長者端不切換

## 語音對話（chat_screen，已實作第一版）
- 免手持迴圈四階段：`idle` → `listening` → `thinking` → `speaking` → 自動再聆聽
- 各階段狀態同時用文字 + 大圖示 + 顏色呈現（不可只靠顏色）
- 底部語音面板圓角 `26 26 0 0`；麥克風圓鈕；氣泡 AI `18 18 18 4`／長者 `18 18 4 18`
- 打字備援永遠可用

> TODO：正式 `/chat`、客語走 audio 送後端辨識。
